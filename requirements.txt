# API layer
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Validation
pydantic>=2.7.0,<3.0.0

# Database — PostgreSQL (build target; mirrors the real Oracle schema subset)
psycopg2-binary>=2.9.9
# Future Oracle adapter (repository swap) — uncomment when building it:
# oracledb>=2.0.0

# Vector DB
chromadb>=0.5.0
fastembed>=0.2.0

# RAM monitoring (seeding)
psutil>=5.9.0

# HTTP client for Groq/OpenRouter API (async-capable)
httpx>=0.27.0

# Environment variables
python-dotenv>=1.0.0

# Prompt templates
jinja2>=3.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
